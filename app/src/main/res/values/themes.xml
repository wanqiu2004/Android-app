<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ViewPage2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ViewPage2" parent="Base.Theme.ViewPage2"/>
    <!-- Base application theme. -->
    <style name="Base.Theme.RecyclerViewStudy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 设置状态栏颜色 -->
        <item name="android:statusBarColor">@color/pink</item>

        <!-- 设置状态栏文字图标颜色为黑（适用于浅色背景） -->
        <item name="android:windowLightStatusBar">true</item>

        <!-- 可选：定义 primary color -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:navigationBarColor">@color/pink</item> <!-- 设置导航栏背景颜色 -->
        <item name="android:windowLightNavigationBar">true</item> <!-- 设置图标为黑色（适用于浅色背景） -->

    </style>

    <style name="Theme.RecyclerViewStudy" parent="Base.Theme.RecyclerViewStudy"/>
</resources>